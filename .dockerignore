# Arquivos e diretórios a ignorar no build do Docker

# Git
.git
.gitignore
.gitattributes

# Python
__pycache__
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
env.bak/
venv.bak/
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Arquivos gerados (serão criados dentro do container)
/media          # Arquivos de mídia enviados pelos usuários (não devem estar na imagem)
/staticfiles    # Arquivos estáticos coletados (gerados por collectstatic dentro do container)
                # Se você quiser incluir staticfiles pré-coletados, remova esta linha

# NOTA: static/ NÃO está aqui porque contém os arquivos FONTE (CSS, JS, imagens)
# que DEVEM estar na imagem para o collectstatic funcionar corretamente

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Documentação
*.md
!README.md
docs/

# Arquivos de ambiente
.env
.env.local
.env.*.local
*.env
env.production          # Não copiar env.production (mesmo que seja exemplo)
env.example
env_example.txt

# Docker
Dockerfile*
docker-compose*.yml
.dockerignore

# Scripts de deploy
deploy.sh
deploy_*.sh
terraform/
*.tf
*.tfvars

# Logs e backups
logs/
*.log
backups/
*.sql.gz

# Arquivos temporários
*.tmp
*.bak
*.swp
.cache/

# SSL (não deve estar no build)
ssl/
*.pem
*.key
*.crt

# Configurações locais
apache_config.conf
nginx_config.conf
*.service

# Arquivos de teste
test_*.html
test_*.py

# Excel e arquivos de dados grandes
*.xlsx
*.xls
*.csv

# IMPORTANTE: migrations/ NÃO está aqui!
# As migrations DEVEM estar na imagem Docker para funcionar corretamente.
# Django precisa das migrations para executar: python manage.py migrate

